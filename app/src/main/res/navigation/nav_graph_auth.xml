<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation.xml"
    app:startDestination="@id/stateFragment">

    <fragment
        android:id="@+id/loginFragment"
        android:name="de.ur.explure.views.LoginFragment"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login">
        <action
            android:id="@+id/navigateToRegister"
            app:destination="@id/registerFragment" />
    </fragment>
    <fragment
        android:id="@+id/registerFragment"
        android:name="de.ur.explure.views.RegisterFragment"
        android:label="fragment_register"
        tools:layout="@layout/fragment_register">
    </fragment>
    <fragment
        android:id="@+id/stateFragment"
        android:name="de.ur.explure.views.StateFragment"
        android:label="StateFragment" >
        <action
            android:id="@+id/navigateAndPopBackStackToLoginFragment"
            app:destination="@id/loginFragment"
            app:popUpTo="@id/stateFragment"
            app:popUpToInclusive="true"
            />

    </fragment>
    <include app:graph="@navigation/nav_graph_host" />

</navigation>