<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/background_route_creation_options_panel"
    android:elevation="24dp"
    android:paddingHorizontal="8dp"
    android:orientation="vertical"
    android:visibility="gone"
    tools:context=".views.MapFragment"
    tools:visibility="visible">

    <!--
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/route_creation_options_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:padding="4dp"
        android:text="Optionen"
        android:textAppearance="@style/TextAppearance.AppCompat.Small"
        android:textStyle="bold" />-->

    <ImageButton
        android:id="@+id/add_marker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="6dp"
        android:background="@drawable/icon_button_border"
        android:padding="8dp"
        android:src="@drawable/ic_baseline_add_location_alt_24"
        android:tint="@color/colorAccent"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/edit_marker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/add_marker_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="6dp"
        android:background="@drawable/icon_button_border"
        android:padding="8dp"
        android:src="@drawable/ic_baseline_edit_location_alt_24"
        android:tint="@color/color_info"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/delete_marker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_marker_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="6dp"
        android:background="@drawable/icon_button_border"
        android:padding="8dp"
        android:src="@drawable/ic_baseline_wrong_location_24"
        android:tint="@color/color_error"
        tools:ignore="ContentDescription" />

    <!-- TODO schöneren ripple effekt als das machen! -->
    <ImageButton
        android:id="@+id/reset_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/delete_marker_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="6dp"
        android:padding="8dp"
        android:background="@drawable/icon_button_border"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_baseline_delete_24"
        android:tint="@color/color_error"
        tools:ignore="ContentDescription" />

    <!-- TODO alternativ auch so möglich mit Text dazu: -->
    <!--
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/delete_marker_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="6dp"
        android:background="@drawable/icon_button_border"
        android:padding="6dp"
        android:drawableTint="@color/colorAccent"
        android:text="Karte"
        app:drawableTopCompat="@drawable/ic_baseline_map_24" />
        -->

</RelativeLayout>
