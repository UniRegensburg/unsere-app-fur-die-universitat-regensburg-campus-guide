<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/background_options_panel"
    android:clickable="true"
    android:elevation="24dp"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingHorizontal="10dp"
    android:visibility="gone"
    tools:visibility="visible">

    <ImageButton
        android:id="@+id/draw_route_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="8dp"
        android:background="@drawable/background_icon_button"
        android:src="@drawable/ic_pencil"
        android:tint="@color/themeColor"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/move_map_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/draw_route_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="8dp"
        android:background="@drawable/background_icon_button"
        android:src="@drawable/ic_move_filled"
        android:tint="@color/colorInfo"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/delete_route_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/move_map_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="8dp"
        android:background="@drawable/background_icon_button"
        android:src="@drawable/ic_pencil_delete"
        android:tint="@color/colorError"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/erase_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/delete_route_button"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="8dp"
        android:background="@drawable/background_icon_button"
        android:src="@drawable/ic_eraser_round"
        android:tint="@color/colorError"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/reset_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/erase_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/background_icon_button"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_baseline_delete_24"
        android:tint="@color/colorError"
        tools:ignore="ContentDescription" />

</RelativeLayout>
